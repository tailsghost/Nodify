<UserControl
    x:Class="Nodify.Views.NodeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="{Binding Width}"
    Height="{Binding Height, Converter={StaticResource HeightWithConnectorsConverter}, ConverterParameter=0}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="{Binding Height}" />
        </Grid.RowDefinitions>
        <Border
            x:Name="Node"
            Background="#BF000000"
            BorderBrush="Black"
            BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="55" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="55" />
                </Grid.ColumnDefinitions>
                <ItemsControl ItemsSource="{Binding Inputs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel VerticalAlignment="Top" Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <Ellipse
                                    Width="{Binding ConnectorSize}"
                                    Height="{Binding ConnectorSize}"
                                    Margin="0,5"
                                    Fill="{Binding Brush}"
                                    PreviewMouseLeftButtonDown="OnConnectorMouseDown"
                                    PreviewMouseLeftButtonUp="OnConnectorMouseUp"
                                    Stroke="Black"
                                    StrokeThickness="1"
                                    Tag="{Binding}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="10"
                                    Foreground="Beige"
                                    Text="{Binding Name}" />
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl Grid.Column="2" ItemsSource="{Binding Outputs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel VerticalAlignment="Top" Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="10"
                                    Foreground="Beige"
                                    Text="{Binding Name}" />
                                <Ellipse
                                    Width="{Binding ConnectorSize}"
                                    Height="{Binding ConnectorSize}"
                                    Margin="0,5"
                                    Fill="{Binding Brush}"
                                    PreviewMouseLeftButtonDown="OnConnectorMouseDown"
                                    PreviewMouseLeftButtonUp="OnConnectorMouseUp"
                                    Stroke="Black"
                                    StrokeThickness="1"
                                    Tag="{Binding}" />
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</UserControl>
